# WindShield — exact-pinned environment for reproducibility.
# Versions verified together on Python 3.14 / Windows.
numpy==2.5.2
pandas==3.0.5
pyarrow==25.0.1
scikit-learn==1.9.0
streamlit==1.63.0
lightgbm==4.7.0
networkx==3.6.1
shap==0.52.0
plotly==7.0.0
pyvis==0.3.2
scipy==1.18.1

# Every optional import in the codebase is guarded by try/except, so the
# pipeline degrades gracefully if an accelerator is missing — a deliberate,
# production-minded design choice: a broken machine degrades to a slower but
# correct numpy fallback instead of crashing at startup.

# ruptures is OPTIONAL and only accelerates changepoint detection; it needs a
# C compiler (Cython) to build, so it is intentionally NOT in the default list
# to keep `pip install -r requirements.txt` reliable on every machine. Without
# it the pipeline uses the built-in numpy changepoint fallback (same results).
#   ruptures>=1.1   # <- uncomment only on a machine with a C compiler

# Dev / tests
pytest==9.1.1
